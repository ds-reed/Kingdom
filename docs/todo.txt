Kingdom TODO (Outstanding Only)
===============================





# 🧱 Master Architectural TODO List

## 1. Create a `VerbHandler` Base Class
- Provide unified helpers:
  - `self.player(ctx)`
  - `self.room(ctx)`
  - `self.game(ctx)`
  - `self.state(ctx)`
  - `self.require_player(ctx)`
- Standard refusal helpers:
  - `self.missing_target(verb)`
  - `self.not_here(noun)`
  - `self.not_in_inventory(noun)`
- Noun/word resolution:
  - `self.resolve_noun_or_word(target, words)`
- ALL-handling:
  - `self.handle_all(ctx, items, single_fn, verb_name)`
- Summary formatting:
  - `self.summary(player, verb, items)`
- Item-location helpers:
  - `self.find_item_location(room, item)`
  - `self.is_accessible(room, item)`
- Message assembly:
  - `self.build_message(*parts)`
- Special-handler pipeline:
  - `self.run_special_handler(target, verb, words, ctx)`

---

## 2. Move World-Logic Helpers Into Models

### Room
- `find_item(item)`
- `all_accessible_items()`
- `items_player_can_take(player)`
- `visible_items()`

### Item
- `can_be_taken_by(player)`
- `can_be_dropped_by(player)`
- `location(room)`
- `is_accessible(room)`

### Box
- `accessible_items()`
- `contains(item)`

### Game
- `current_room` shortcut
- `player_has(item)`
- `find_room_by_name_or_number(query)`

---

## 3. Consolidate Shared Helper Functions
Move or integrate:
- `basic_checks`
- `require_item`
- `apply_state_change`

Into:
- `verb_utils.py`  
  **or**
- the `VerbHandler` base class

---

## 4. Normalize Message Assembly
Create a unified helper:
- `self.build_message(*parts)`

Used by:
- state verbs  
- inventory verbs  
- movement verbs  
- world verbs  

---

## 5. Extract a `MovementEngine`
Centralize:
- direction resolution
- exit lookup
- scoring
- visited marking
- rendering
- movement error patterns

`perform_movement` becomes the core engine.

---

## 6. Extract a `TeleportResolver`
Centralize teleport logic:
- noun target resolution
- numeric index resolution
- partial name matching
- ambiguous match detection
- room listing

Likely belongs in `Game`.

---

## 7. Unify the `help` Verb
Two copies exist:
- MetaVerbHandler.help
- WorldVerbHandler.help

Create a single implementation.

---

## 8. Normalize Special Handler Pipeline
Create:
- `self.run_special_handler(target, verb, words, ctx)`

Used by:
- open/close/unlock/light/extinguish/eat/rub/say/make
- future puzzle verbs

---

## 9. Normalize Post-Mutation Hooks
Create optional hooks:
- `after_open(target, ctx)`
- `after_close(target, ctx)`
- `after_eat(target, ctx)`
- `after_light(target, ctx)`
- etc.

Handlers call them; subclasses override as needed.

---

## 10. Establish the VerbHandler Hierarchy
All handlers subclass the base class:

```
VerbHandler
 ├── InventoryVerbHandler
 ├── MovementVerbHandler
 ├── StateVerbHandler
 ├── MetaVerbHandler
 ├── UIVerbHandler
 └── WorldVerbHandler
```

Ensures:
- shared utilities
- consistent signatures
- rediscoverable architecture
- thinner, cleaner handlers



---- remove dead code throughout

1) Next session priority: Fix issues from testing
    [x] Cave was only giving short description so no mention of the pool after first entry 
    [x] Look should always show long description. (is this a general issue?)   
    [x] minor - room description for cave includes gold bar. Gold bar is a movable object
    [x] --mode modern should not use all caps

2) Next session priority: Improve game play mechanics issues turned up in tests
    [x] important - some directions are hidden and should not be seen by "look" or room descriptions
    [x]The pool in the cave shouldn't be shown as an exit even though it is.
    [ ] Paper bag should be carriable in inventory. Need to enhance box class
    [ ] think about how to handle inferred nouns more generally. unlock the only unlockable for example

3) Update document files
    [ ] update project_structure.md to match current structure
    [ ] update readme as needed

4) Next session priority: Wardens
    [ ] Design warden entities, patrol behavior, and room placement in canonical data
    [ ] Add warden interaction/encounter mechanics (detection, consequences, messaging)
    [ ] Validate warden flow against existing movement, dark-room, and death/recovery systems

5) Lighting polish
    [x] Add extinguish verb for light sources (`extinguish torch`, `put out torch`)
    [x] Decide whether lit non-torch items in inventory should illuminate (currently: lit torch only)

6) Minigame integration
    [ ] Standardize room/minigame initialization path
    [ ] Define lightweight minigame contract (start, result, side effects)
    [ ] Add one end-to-end sample minigame beyond placeholders

7) Parser/UX improvements
    [ ] Improve multi-word noun matching and ambiguity handling
    [ ] Add command history / up-arrow recall

8) Optional terminal enhancements (deferred)
    [ ] Sticky status/banner line via ANSI cursor control
    [ ] Live CAPS rewriting while typing in TRS80 mode

9) Real content wiring (wait for canonical data file)
    [ ] Replace temporary mechanics-test rooms/items with canonical world data
    [ ] Re-map room/item IDs and noun aliases to final data-file naming
    [ ] Re-validate trapdoor/lighting/Djinni/swim flows against canonical layout

10) Gameplay guidance/scoring (deferred)
    [ ] Add optional hint system hooks (data-driven, disabled by default)
    [ ] Add scoring model and event hooks (treasures, deaths, milestones)
    [ ] Add end-game summary output (score, major achievements, completion state)

11) Verb/synonym safety (deferred)
    [ ] Add collision check so verb synonyms cannot silently shadow common nouns
    [ ] Add quick startup warning/log entry for any verb/noun token conflicts

12) Tomorrow design session: container-capable items (bag/revolver)
    [ ] Decide architecture: shared container capability for Item + Box (preferred) vs making Box a direct Item subclass
    [ ] Define MVP behavior for carriable containers (paper bag in inventory; open/close; put/take from carried container)
    [ ] Define revolver model as a container-like item with capacity 6 and action hooks (load/unload/fire integration later)
    [ ] Plan migration path that preserves current JSON and game behavior while adding new capabilities incrementally
    [ ] Keep `unlock` as compatibility verb while evolving `insert`/`turn` into object-specific behavior

13) Legacy verb implementation notes captured (2026-02-20)
    [ ] throw: inventory item throw with optional target (`throw item`, `throw item at target`); default no-effect messaging + target hooks
    [ ] read: readable objects (`read_text`, optional one-time clue text) for books/signs/notes
    [ ] kick: flavor action with optional breakable interaction; can reuse `is_broken` behavior
    [ ] knock: ambient "knock on" support now; future "knock out" routed through warden/combat system
    [ ] shoot: revolver-based action with ammo checks; allow `shoot` (in air) and `shoot target`
    [ ] turn: generic rotational puzzle verb (victrola crank now, turn-key flow later)
    [ ] push/press: button/switch interactions with toggle/trigger hooks
    [ ] dial: retro phone interactions with number map (`dial number`) and fallback/busy messaging
    [ ] break/smash: breakable-object support with reveal/effect hooks
    [ ] drink + upgraded eat: add hero size state and size-gated room/exit behavior
    [ ] drag: move heavy/draggable entities (ex: warden body), later with directional drag and consequences
    [ ] kill: warden-focused hostile action with target-specific outcomes and penalties
    [ ] swing: traversal verb like swim (rope/chasm), can use hidden exit pathing
    [ ] untie/tie: rope state transitions controlling traversal and puzzle state







# TODO — Unify Item Attribute Handling with ITEM_FIELDS Table

## 1. Create a single source of truth for item attributes
- [ ] Add a new module or section in `item.py` defining:
  ```python
  ITEM_FIELDS = {
      "pickupable": True,
      "refuse_string": None,
      "presence_string": None,
      "is_openable": False,
      "is_open": False,
      "is_lockable": False,
      "is_locked": False,
      "unlock_key": None,
      "is_lightable": False,
      "is_lit": False,
      "can_ignite": False,
      "ignite_success_string": None,
      "rubbable": False,
      "rubbed_name": None,
      "rubbed_presence_string": None,
      "rub_success_string": None,
      "rub_repeat_string": None,
      "rub_trigger_room": None,
      "rub_minigame_tease": None,
      "too_heavy_to_swim": False,
      "edible": False,
      "eat_refuse_string": None,
      "eat_success_string": None,
      "eat_missing_string": None,
      # Add more as needed
  }
  ```

## 2. Refactor Item.__init__ to use ITEM_FIELDS
- [ ] Replace the long parameter list with:
  ```python
  def __init__(self, name, noun_name=None, behavior_ids=None, special_handlers=None, **kwargs):
  ```
- [ ] Loop through ITEM_FIELDS and set attributes:
  ```python
  for field, default in ITEM_FIELDS.items():
      setattr(self, field, kwargs.get(field, default))
  ```

## 3. Update the world loader to pass JSON directly
- [ ] Replace explicit constructor args with:
  ```python
  item = Item(**item_spec)
  ```
- [ ] Ensure name/noun_name/behavior_ids/special_handlers are still handled explicitly.

## 4. Migrate existing JSON
- [ ] Remove fields that were previously ignored or duplicated.
- [ ] Ensure all item capability flags match ITEM_FIELDS keys.
- [ ] Validate that no JSON fields are silently dropped.

## 5. Add validation (optional but recommended)
- [ ] Add a check in Item.__init__ to warn about unknown JSON keys.
- [ ] Add type coercion (bool, str, etc.) if needed.

## 6. Update debugging tools
- [ ] Update your debug verb to print ITEM_FIELDS dynamically.
- [ ] Add a helper to pretty-print only fields that differ from defaults.

## 7. Remove old constructor parameters
- [ ] Delete unused parameters from Item.__init__.
- [ ] Remove redundant loader logic.
- [ ] Clean up any code paths that referenced old attributes.

## 8. Regression test verbs
- [ ] Test open/close
- [ ] Test lock/unlock
- [ ] Test light/extinguish
- [ ] Test rub/eat (if implemented)
- [ ] Test special_handlers overrides

When loading the world, maintain a global registry of item identities keyed by (room_name, noun_name) or a unique item ID.

If the loader encounters the same noun twice:

warn

or dedupe

or merge

or refuse to load

Ensure that items in the player’s inventory are removed from their original rooms.

Ensure that rooms do not contain items that have been moved into containers.

## 9. Celebrate
- [ ] Enjoy the fact that adding a new item attribute now requires updating **ONE** place instead of nine.

# potential json simplification 

Summary commentary (for your document)Unlisted verbs → automatic default/generic behavior ("You can't X that", or standard state change if applicable)
actions_refused → explicitly blocks a verb + provides custom refusal text (replaces old flags like is_gettable: false + get_refuse_string)
actions_handled → marks a verb as having custom logic + names the handler function (replaces old special_handlers dict)
state_descriptions → passive flavor text based on current state (used in look/examine/render)
Minimalism → most items have empty or missing actions_refused/handled sections — no need to list every verb
Extensibility → easily add new verbs by adding one entry in one section on one item
Deprecation note → old flags (is_gettable, is_openable, etc.) can be phased out over time in favor of actions_refused.get.enabled = false / actions_handled.open.enabled = true

This structure should give you a good, documented starting point for your notes. Let me know when you're ready to implement loading/parsing for it, or if you'd like a version with one or two more fields added (e.g. required_item, score_on_discovery, etc.). 

Option 3 – Dedicated data classes (my strong recommendation for where you're heading)Create small dataclasses for each verb's configuration, then attach them to the item.

      from dataclasses import dataclass, field
      from typing import Optional, Callable, Any

      @dataclass
      class VerbConfig:
          enabled: bool = True
          handler: Optional[str] = None               # name of special handler function
          messages: dict[str, str] = field(default_factory=dict)
          requires: dict[str, Any] = field(default_factory=dict)  # e.g. {"unlocked": True, "item": "key"}

      @dataclass
      class ItemConfig:
          name: str
          noun_name: str
          is_gettable: bool = True
          actions: dict[str, VerbConfig] = field(default_factory=dict)
          state_descriptions: dict[str, str] = field(default_factory=dict)

  # based on the json below. Not issues unresolved in below - want to have default handlers if not explicitly mentioned as enabled or refused.
  # also need a concept of a shared state between the verbs that toggle the same state (open,close), (light, extinguish
  # idea - may organize around states insead i.e. open (Open sets true, Close sets false, open_action message, close_action message, open_state message, close_state message)
  # but try to remove the verb names from the messages

  # another idea  -  just adopt a unified naming scheme for now, even if it isn't proper grammer
     is_open, can_open, is_open_msg, is_open_not_msg, open_action_msg, open_not_action_msg, is_light, is_not_light, can_light, is_eat, can_eat, 

    {
        "name": "rusty iron trapdoor",
        "noun_name": "trapdoor",
        "is_gettable": false,

        "actions": {
            "open": {
                "enabled": true,
                "handler": "open_trapdoor",
                "messages": {
                    "success": "The trapdoor creaks open.",
                    "already": "It's already open."
                },
                "requires": {
                    "unlocked": true
                }
            },
            "close": {
                "enabled": true,
                "messages": {
                    "success": "You slam the trapdoor shut."
                }
            },
            "unlock": {
                "enabled": true,
                "messages": {
                    "fail_no_key": "The trapdoor is locked tight. You'll need a key."
                },
                "requires": {
                    "item": "small brass key"
                }
            },
            "eat": {
                "enabled": false,
                "messages": {
                    "fail": "You can't eat iron."
                }
            }
        },

        "state_descriptions": {
            "locked": "a rusty iron trapdoor... closed and locked.",
            "unlocked_closed": "...closed but unlocked.",
            "open": "...open, revealing a dark shaft below."
        }
    }


#loading code becomes
      item.actions = {}
      for verb_name, data in item_data.get("actions", {}).items():
          item.actions[verb_name] = VerbConfig(**data)

# usage in handler

      config = target.actions.get("open")
      if not config or not config.enabled:
          return self.msg(f"You can't open the {target.get_noun_name()}.")

      if handler_name := config.handler:
          outcome = try_item_special_handler(target, "open", words, ctx, handler_name)
          if outcome:
              return outcome

      # Default open logic...
      if config.requires.get("unlocked") and getattr(target, "is_locked", False):
          return self.msg(config.messages.get("locked") or "It's locked.")



# after refinement discussion


{
    "name": "a rusty iron trapdoor",
    "noun_name": "trapdoor",

    "can_get": false,

    "can_open": true,
    "can_rev_open": true,
    "is_open": false,

    "open_action_msg": "The trapdoor creaks open, revealing a dark shaft below.",
    "open_already_msg": "It's already open.",
    "open_rev_action_msg": "You slam the trapdoor shut.",
    "open_rev_already_msg": "It's already closed.",

    "can_lock": true,
    "can_rev_lock": true,
    "is_locked": true,

    "lock_action_msg": "You lock the trapdoor securely.",
    "lock_already_msg": "It's already locked.", 
    "lock_rev_action_msg": "The lock clicks open.", 
    "lock_rev_already_msg": "It's already unlocked.",

    "state_descriptions": {
        "locked": "a rusty iron trapdoor in the middle of the floor. The trap door is closed and locked.",
        "not_locked": "a rusty iron trapdoor in the middle of the floor. The trap door is closed but unlocked.",
        "open": "a rusty iron trapdoor in the middle of the floor. The trap door is open.",
        "not_open": "a rusty iron trapdoor in the middle of the floor. The trap door is closed."
    },

    "open_exit_direction": "down",
    "open_exit_destination": "Blank Chamber"
}


### Summary of Naming Patterns Discussion (Final Leaning)

**Core Goal**  
Create readable, consistent, beautiful English-like state keys for `state_descriptions` while preserving uniformity in code (e.g. `can_open`, `is_open`) and avoiding invented words like "breaked".

**Final Preferred Direction**  
- Use **real English past participles / adjectives** in state keys (not base forms or computerish "not_open")
- Prefer natural-sounding words even when irregular ("lit" not "lighted", "broken" not "breaked", "worn" not "weared")
- Use **comma-separated** participles for combined states (e.g. open + lock)
- Order: usually most salient state first (open/closed before locked/unlocked)

**Leaning Toward These Key Styles**
- "opened,locked"
- "not_opened,locked"
- "not_opened,unlocked"
- "opened"                 (when lock state irrelevant or impossible)
- "lit"
- "not_lit"
- "broken"
- "not_broken"
- "worn"

**Why this won over alternatives**
- Avoids fake words ("breaked", "eated") → preserves English beauty
- More natural than base forms ("open,locked") or boolean-ish ("state:open=false,locked=true")
- Still machine-friendly and consistent (comma separator, predictable generation)
- Handles irregular verbs gracefully via small central lookup table

**Central Lookup Table (VERB_MUTATIONS)**
- Static dict loaded at init
- Maps base verb → positive/negative forms + already-message adjectives
- Example:
  - "open":   positive="opened", negative="closed", already_positive="open", already_negative="closed"
  - "light":  positive="lit",    negative="extinguished" / "not_lit"
  - "break":  positive="broken", negative="intact" / "not_broken"
- Regular verbs can fall back to auto-generated "verb + 'ed'" / "not_ + verb + 'ed'"
- Used for:
  - Generating state keys at runtime
  - Building natural "already" messages ("It's already lit.", "It's already broken.")

**Scope**
- Only applies to **persistent, reversible state verbs** (open/close, lock/unlock, light/extinguish, wear/remove, break/repair?, etc.)
- One-way destructive actions (eat, drink, destroy) stay outside this pattern — no persistent "eaten" or "drinked" states

**Overall Philosophy**
Prioritize readable, natural English in JSON keys and messages  
→ accept small, centralized exceptions for irregular verbs  
→ keep `can_verb` / `is_verb` uniformity in attributes  
→ generate keys/messages from lookup rather than hardcoding participles everywhere




{
  "name": "Heavy Stone Door",
  "noun_name": "door",

  "description": "A massive stone door carved with ancient runes blocks the way north.",

  "can_get": false,

  // Open / Close
  "can_open": true,
  "is_open": false,

  "open": {
    "msg": "With a low grinding sound, the heavy stone door swings open.",
    "already_msg": "The door is already open.",
    "rev_msg": "You push the massive door closed with effort.",
    "rev_already_msg": "The door is already closed."
  },

  // Lock / Unlock
  "can_lock": true,
  "is_locked": true,

  "lock": {
    "msg": "You turn the heavy iron key and hear a satisfying click as the door locks.",
    "already_msg": "The door is already locked.",
    "rev_msg": "The lock mechanism releases with a dull thunk.",
    "rev_already_msg": "The door is already unlocked."
  },

  // State-dependent descriptions
  "state_descriptions": {
    "opened": "The heavy stone door stands open, revealing a dark corridor beyond.",
    "not_opened,locked": "The heavy stone door is closed and firmly locked.",
    "not_opened,unlocked": "The heavy stone door is closed but appears unlocked.",
    "opened,locked": "The heavy stone door is open, though strangely still locked in place.",
    "default": "A massive stone door carved with ancient runes blocks the way north."
  },

  // Side effect when opened
  "open_exit": {
    "direction": "north",
    "destination": "Ancient Corridor",
    "rev_hides": true
  },

  // Optional: custom refusal or extra flavor
  "open_refuse_if_locked": "The door won't budge — it's locked tight.",
  "examine_locked": "The door is secured with a large iron lock that looks very old."
}