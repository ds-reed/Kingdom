- Support duplicate handles for noun items
- Support presence in multiple rooms for noun items
- Parser should not resolve noun entries. Just find first match from lexicon. use it as noun token, and then interpretter should match further using room context.
- Interpreter should resolve nouns for both direct and indirect objects
- verbs/exector should use CommandOutcome to pass back status
- enrich the textutils to suppor
    - add correct indirect or indirect article
    - strips duplicate articles
    - remove '..' if found and turn '....' to '...'
    - master function that processes each sentence in output:
        - Add appropriate article
        - Sentence case
        - if bulleted list, capitalize first
        - trailing punctuation
        - look for and remove redundancy (strecth goal)  - "the bright banana looks like a bright banana -> it looks like a bright banana"
- process CommandOutcome messages through text utils unless marked as rendered already
- Overloaded verbs (like drop and look) should be seperated and should use helper functions for overlapping behavior
- spawn_item_to_room should update lexicon
    - lexicon should take item=Item and update=True
    - Item, Room and Container has_item should get has_item_by_alias that looks for name and synonym and returns ObjectID

- Create new portal class for doors/trapdoors/hatches
- Support random encounters
    - New NPC class?
    - random probablilities for encounter (lighted and dark state for room)

Long term list
- world builder
- enrich availble behavior functions for special_handler objects
- mini-game launch
- more safety checks to avoid crashes